# UNVERIFIED PLACEHOLDER DATA — see fixtures/README.md. Postgres-only
# (certification_rules table), consumed by the Phase 3 certification
# mapper. Deliberately only "LED Street Lighting" has a rule here — the
# other two categories in this fixture ("Structural & Materials",
# "Electrical Installation") are left unmapped on purpose, so the mapper's
# fallback to `scheme: UNKNOWN` is exercised by real fixture data rather
# than a special case in code.

- product_category: "LED Street Lighting"
  standard_is_number: null   # applies category-wide, not to one standard
  scheme: ISI
  mandatory: true
  required_evidence:
    - "CM/L licence number"
    - "IS 10322 (Part 5/Sec 1) test report from a BIS-recognised lab"
  notification_reference: "BIS placeholder notification — to be confirmed by Teammate 1"
  effective_date: "2021-01-01"
  source_url: null
  verified: false
