"""Shared contracts between Part 2/4 (upstream/downstream) and Part 3/5.

Deliberately a standalone top-level package (see pyproject.toml) so it can
later be lifted to a shared `packages/contracts/` without any import in this
service, or a teammate's service, having to change.
"""
